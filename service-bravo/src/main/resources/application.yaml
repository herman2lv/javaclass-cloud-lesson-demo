spring:
  datasource:
    url: jdbc:postgresql://service-bravo-database:5432/bravo
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
